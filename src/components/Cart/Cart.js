import React from 'react';
import './Cart.css'

const Cart = ({ cart }) => {

    return (
        <div className='cart'>
            <h2 className='cart-title'><i>Selected items</i></h2>

        </div>
    );
};

export default Cart;